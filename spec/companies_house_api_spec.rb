require 'spec_helper'

describe CompaniesHouseApi do
  it 'has a version number' do
    expect(CompaniesHouseApi::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
